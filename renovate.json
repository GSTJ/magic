{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "This repo's own Renovate config. The shared preset lives in default.json — that is the file Renovate resolves \"github>GSTJ/magic\" to, and serving a preset from renovate.json is deprecated.",
  "extends": ["local>GSTJ/magic"]
}
