// `typescript/array-type` is not named in any preset's `rules`. It is on solely
// because `categories.pedantic` is `error`, which makes it the probe for
// whether `categories` survives a consumption path.
export const names: Array<string> = [];
