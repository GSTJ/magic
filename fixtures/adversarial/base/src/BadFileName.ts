// Deliberately misnamed. `unicorn/filename-case` is on at kebabCase in `base`
// as of 2026-07-27, and the diagnostic's `help` is what magic-kebab reads to
// pick the target name — so this fixture pins both the firing and the target.
export const badFileName = "BadFileName.ts";
